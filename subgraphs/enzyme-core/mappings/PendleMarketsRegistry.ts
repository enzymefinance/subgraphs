import { useVault } from '../entities/Vault';
// import {MarketForUserUpdated } from '../generated/contracts/PendleMarketsRegistryEvents';
import { ensurePendleV2AllowedMarket, usePendleV2AllowedMarket } from '../entities/PendleV2Position';

export function handleMarketForUserUpdated(): void {
// event: MarketForUserUpdated
  // let market = ensurePendleV2AllowedMarket(event.params.user, event.params.marketAddress);
  // let duration = event.params.duration.toI32();
  // market.duration = duration;
  // market.active = duration > 0;
  // market.save();
}
