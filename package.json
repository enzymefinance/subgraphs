{
  "private": true,
  "scripts": {
    "codegen": "graph codegen --debug --output-dir src/types/",
    "build": "graph build --debug",
    "create": "graph create fubhy/melon --node https://api.thegraph.com/deploy/",
    "create-local": "graph create fubhy/melon --node http://127.0.0.1:8020",
    "deploy": "graph deploy fubhy/melon --debug --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "deploy-local": "graph deploy fubhy/melon --debug --ipfs http://localhost:5001 --node http://127.0.0.1:8020"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.11.2",
    "@graphprotocol/graph-ts": "^0.11.0",
    "@melonproject/protocol": "^1.0.8"
  }
}
